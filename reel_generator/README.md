# IntelligentEventReelGenerator (scaffold)

This repository contains a scaffold for the IntelligentEventReelGenerator module described in the design spec.

Commands:
- python -m reel_generator.main <image_folder> "Event prompt" -d 30 -o reel.mp4

Next steps:
- Implement real NIMA/CLIP/YOLO modules
- Integrate MoviePy reel builder
- Add unit and integration tests
